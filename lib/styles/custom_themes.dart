import 'dart:ui';

import 'package:flutter/material.dart';

import 'colors.dart';
import 'app_text_styles.dart';
/*
const kTextTheme = TextTheme(
  subtitle1: kRobotoReg14Color1F1F1F,
);

const kAppBarTheme = AppBarTheme(
  centerTitle: true,
  color: Colors.white,
  elevation: 6,
  brightness: Brightness.light,
  iconTheme: IconThemeData(
    color: kColor2F2F2F,
  ),
  textTheme: TextTheme(headline6: kRobotoMed18ColorBlack),
);

const kSliderThemeData = SliderThemeData(
  trackHeight: 2,
  activeTickMarkColor: Colors.green,
  activeTrackColor: kColorFF4980,
  inactiveTrackColor: kColorC4C4C4,
  thumbColor: kColorFF0550,
  thumbShape: RoundSliderThumbShape(enabledThumbRadius: 6),
  rangeThumbShape: RoundRangeSliderThumbShape(enabledThumbRadius: 6),
);

const kInputDecorationTheme = InputDecorationTheme(
  filled: true,
  fillColor: kColorEBEFF2,
  contentPadding: EdgeInsets.only(left: 24, top: 14, bottom: 14, right: 24),
  labelStyle: kRobotoReg14Color1F1F1F,
  hintStyle: kRobotoLight14Color777777,
  focusedBorder: kInputDecorationBorder,
  disabledBorder: kInputDecorationBorder,
  errorMaxLines: 4,
  errorBorder: kInputDecorationBorder,
  enabledBorder: kInputDecorationBorder,
  focusedErrorBorder: kInputDecorationBorder,
  border: kInputDecorationBorder,
);

const kInputDecorationBorder = OutlineInputBorder(
  borderRadius: BorderRadius.all(
    Radius.circular(51),
  ),
  borderSide: BorderSide.none,
);

const kInputDecorationBorder2 = OutlineInputBorder(
  borderRadius: BorderRadius.all(
    Radius.circular(6),
  ),
  borderSide: BorderSide.none,
);

const kInputDecorationTheme2 = InputDecoration(
  contentPadding: EdgeInsets.symmetric(horizontal: 8, vertical: 20),
  hintText: "Комментарии",
  border: OutlineInputBorder(
      borderSide: BorderSide(
          color: kColorEBEFF2,
          width: 0),
      borderRadius: BorderRadius.all(Radius.circular(16))
  ),
  errorBorder: InputBorder.none,
  focusedBorder:
  OutlineInputBorder(
      borderSide: BorderSide(
          color: kColorEBEFF2,
          width: 0),
      borderRadius: BorderRadius.all(Radius.circular(16))
  ),
  disabledBorder:
  OutlineInputBorder(
      borderSide: BorderSide(
          color: kColorEBEFF2,
          width: 0),
      borderRadius: BorderRadius.all(Radius.circular(16))
  ),

  enabledBorder:
  OutlineInputBorder(
      borderSide: BorderSide(
          color: kColorEBEFF2,
          width: 0),
      borderRadius: BorderRadius.all(Radius.circular(16))
  ),
  focusedErrorBorder:
  InputBorder.none,
);



var kElevationButtonTheme = ElevatedButtonThemeData(
  style: ButtonStyle(
    minimumSize: MaterialStateProperty.all(Size(207, 45)),
    textStyle: MaterialStateProperty.all(kRobotoMed14ColorWhite),
    foregroundColor: MaterialStateProperty.all(Colors.white),
    backgroundColor: MaterialStateProperty.all(kColorFF0550),
    shape: MaterialStateProperty.all(
      RoundedRectangleBorder(
        borderRadius: BorderRadius.all(
          Radius.circular(10.0),
        ),
      ),
    ),
  ),
);

var kAlertElevatedButtonStyle = ButtonStyle(
  minimumSize: MaterialStateProperty.all(Size(20, 30)),
  textStyle: MaterialStateProperty.all(kRobotoMed14ColorWhite),
  foregroundColor: MaterialStateProperty.all(Colors.white),
  backgroundColor: MaterialStateProperty.all(kColorFF0550),
  shape: MaterialStateProperty.all(
    RoundedRectangleBorder(
      borderRadius: BorderRadius.all(
        Radius.circular(10.0),
      ),
    ),
  ),
);
*/